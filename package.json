{
  "name": "digitara-landing-page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "php artisan serve --host=127.0.0.1 --port=8000",
    "test": "php artisan test"
  },
  "dependencies": {},
  "devDependencies": {}
}
